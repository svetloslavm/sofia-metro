public/NEONDSSampleLiDARPointCloud.las filter=lfs diff=lfs merge=lfs -text
public/West_Coast_LiDAR.laz filter=lfs diff=lfs merge=lfs -text

# Normalize line endings
* text=auto

# Treat binary files as binary
*.png binary
*.jpg binary
*.gif binary
*.pdf binary

# Custom diff for specific file types
*.json diff=json
*.lock -diff

# Exclude files from export
.env export-ignore
